#include<stdio.h>
int main()
{
	int a,b,c,determinant;
	printf("determinant of a,b and c:");
	scanf("%d%d%d",&a,&b,&c);
	determinant=(b*b)-(4*a*c);
	printf("the determinant i:%d",determinant);
	return 0;
}
